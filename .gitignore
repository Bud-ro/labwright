# Dart
.dart_tool/
.packages
pubspec.lock
doc/api/

# Zig
zig-out/
.zig-cache/

# Native build artifacts
*.o
*.obj
*.so
*.dylib
*.dll
*.a
*.lib

# Sandbox / editor mount artifacts (not part of the repo)
.claude/
.mcp.json
.DS_Store

# Fetched VI corpus — clean-room + licensing, so the .vi files are NOT committed.
# Reproduce with `dart run packages/labwright_videcode/tool/fetch_corpus.dart`.
# Kept at the repo root (not /tmp) for visibility into what tests/probes consume.
/vi-corpus/

# Fetched TestStand corpus — same clean-room + licensing reason as vi-corpus.
# Reproduce with `dart run packages/labwright_teststand/tool/fetch_seq_corpus.dart`.
/seq-corpus/
