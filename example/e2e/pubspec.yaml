name: e2e_test
description: The labwright E2E example — a simulated PSU board suite on the package:labwright runner, plus the measurement/TDMS layer a bench team builds on top.
publish_to: none
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  labwright:
    path: ../../packages/labwright
  labwright_tdms:
    path: ../../packages/labwright_tdms

dev_dependencies:
  lints: ^5.0.0
